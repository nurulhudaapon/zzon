import { parse } from './parse';
import { stringify } from './stringify';

/**
 * The ZON object provides two methods for parsing and stringifying ZON data.
 */
export const ZON = {
  parse,
  stringify,
};
